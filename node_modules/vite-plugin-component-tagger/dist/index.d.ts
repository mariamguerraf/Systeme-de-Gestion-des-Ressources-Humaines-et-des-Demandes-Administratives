import type { Plugin } from 'vite';
export interface ComponentTaggerOptions {
    /**
     * Type of tagging to perform
     * - 'components': Tag only the top-level element of each component (default)
     * - 'elements': Tag all elements in the component
     * @default 'components'
     */
    tagType?: 'components' | 'elements';
    /**
     * Whether to enable in production
     * @default false
     */
    enableInProd?: boolean;
    /**
     * File extensions to process
     * @default ['.svelte']
     */
    extensions?: string[];
}
export default function componentTagger(options?: ComponentTaggerOptions): Plugin;
